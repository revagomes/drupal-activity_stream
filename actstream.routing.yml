activity_stream.default_page:
  path: '/activity-stream'
  defaults:
    _controller: '\Drupal\activity_stream\Controller\ActivityStreamController::defaultPage'
    _title: 'Activity Stream'
  requirements:
    _permission: 'access content'

activity_stream.user_page:
  path: '/user/{user}/activity-stream'
  defaults:
    _controller: '\Drupal\activity_stream\Controller\ActivityStreamController::userPage'
    _title_callback: '\Drupal\activity_stream\Controller\ActivityStreamController::userPageTitle'
  requirements:
    _permission: 'access content'
  options:
    parameters:
      user:
        type: entity:user

activity_stream.user_accounts:
  path: '/user/{user}/edit/activity-stream'
  defaults:
    _form: '\Drupal\activity_stream\Form\ActivityStreamAccountsForm'
    _title: 'Activity Stream accounts'
  requirements:
    _permission: 'access content'
  options:
    parameters:
      user:
        type: entity:user

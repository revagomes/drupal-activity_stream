actstream_wall.settings:
  type: config_object
  label: 'Activity Stream Social Wall settings'
  mapping:
    refresh_interval:
      type: integer
      label: 'JS refresh interval in milliseconds'
    items_per_page:
      type: integer
      label: 'Items per page'
